# 高精度电子秒表 — 开发任务书

## 一、项目概述

基于 STM32F103C8T6 设计一个高精度电子秒表，使用 4 个独立共阴极七段数码管显示时间，时间基准由 TIM2 定时器提供 100us 中断。

## 二、硬件平台

| 项目 | 说明 |
|------|------|
| MCU | STM32F103C8T6 (Cortex-M3, 72MHz) |
| 数码管 | 4 个独立共阴极七段数码管（LED1 / LED8 / LED9 / LED10） |
| 驱动方式 | GPIO 高电平 → S8050 三极管 → 点亮段 |
| 扫描方式 | 无动态扫描、无位选，每管独立 GPIO 控制 |

## 三、数码管 GPIO 映射

### LED1（PA0~PA6）

| 段 | A | B | C | D | E | F | G |
|----|---|---|---|---|---|---|---|
| GPIO | PA0 | PA1 | PA2 | PA3 | PA4 | PA5 | PA6 |

### LED8（PA7~PA12, PA15）

| 段 | A | B | C | D | E | F | G |
|----|---|---|---|---|---|---|---|
| GPIO | PA7 | PA8 | PA9 | PA10 | PA11 | PA12 | PA15 |

### LED9（PB1, PB3~PB8）

| 段 | A | B | C | D | E | F | G |
|----|---|---|---|---|---|---|---|
| GPIO | PB1 | PB3 | PB4 | PB5 | PB6 | PB7 | PB8 |

### LED10（PB9, PB12~PB15, PC13~PC15）

| 段 | A | B | C | D | E | F | G | DP |
|----|---|---|---|---|---|---|---|---|
| GPIO | PB9 | PC13 | PB13 | PB14 | PB15 | PC14 | PB12 | PC15 |

> 注：LED10 映射经硬件实测修正，与原理图原始标注不同：
> - B 段由 PC13 驱动（原标注 PB12）
> - F 段由 PC14 驱动（原标注 PC13）
> - G 段由 PB12 驱动（原标注 PC14）

## 四、功能要求

1. TIM2 定时器 100us 更新中断
2. 每 10 次中断（1ms）total_ms++
3. 四位数码管显示格式：秒.毫秒（如 12.345s）

| 数码管 | 显示内容 | 变化周期 |
|--------|---------|---------|
| LED10 | 秒个位 (total_ms/1000)%10 | 1s |
| LED9  | 百毫秒位 (total_ms/100)%10 | 100ms |
| LED8  | 十毫秒位 (total_ms/10)%10 | 10ms |
| LED1  | 毫秒个位 (total_ms/1)%10 | 1ms |

4. LED10 小数点（PC15）常亮
5. 中断中仅做计时变量更新，不做 GPIO 刷新

## 五、技术约束

- 使用 STM32F10x 标准外设库（StdPeriph）
- 禁止 HAL 库、禁止 CubeMX 生成代码
- 使用 GPIO_Init / GPIO_SetBits / GPIO_ResetBits
- 使用 RCC_APB2PeriphClockCmd 使能时钟
- 中断共享变量使用 volatile 修饰

## 六、TIM2 配置计算

| 参数 | 值 | 计算 |
|------|----|------|
| TIM2 时钟 | 72 MHz | APB1(36MHz) × 2 |
| 预分频 PSC | 71 | 72MHz / (71+1) = 1MHz |
| 自动重装 ARR | 99 | (99+1) × 1us = 100us |
| 中断频率 | 10 kHz | 每秒 10000 次中断 |

## 七、交付文件

| 文件 | 说明 |
|------|------|
| main.c | 主循环，刷新四位数码管 |
| stm32f10x_it.c | TIM2_IRQHandler，计时变量更新 |
| timer.c / timer.h | TIM2 初始化 |
| gpio.c / gpio.h | GPIO 初始化 + 七段显示函数 |
| delay.c / delay.h | SysTick 延时（备用） |

## 八、验收标准

1. 秒表上电自动计时
2. 毫秒位每 1ms 变化，十毫秒位每 10ms 变化，百毫秒位每 100ms 变化，秒位每 1s 变化
3. 四个数码管同时正常显示，LED10 小数点常亮
4. 编译无错误无警告
