# 高精度电子秒表 — 开发报告

## 一、项目总结

本项目基于 STM32F103C8T6 与标准外设库（StdPeriph），实现了一个以 TIM2 100us 中断为时间基准的高精度电子秒表。4 个独立共阴极七段数码管显示「秒.毫秒」格式，全程未使用 HAL 库、CubeMX、动态扫描或位选。

## 二、开发过程

### 2.1 工程搭建

基于已有 STM32F103C8T6 工程模板，确认标准外设库、CMSIS 核心文件、startup 启动文件与 Keil MDK 工程文件齐全。

### 2.2 系统时钟修正

**问题**：system_stm32f10x.c 中同时定义了 SYSCLK_FREQ_24MHz 与 SYSCLK_FREQ_72MHz，由于 #elif 链按顺序匹配，系统实际运行在 24MHz 而非 72MHz，导致 TIM2 定时周期偏差 3 倍。

**解决**：注释掉 SYSCLK_FREQ_24MHz，仅保留 SYSCLK_FREQ_72MHz，系统时钟恢复 72MHz。

### 2.3 GPIO 引脚映射实测与修正

**问题**：原理图标注的 LED10 引脚映射与实际 PCB 走线不一致，导致数码管显示错误。

**排查方法**：编写逐引脚扫描程序（每次仅置高一个引脚，其余拉低，2 秒切换），配合万用表测量与目测，逐一确认每个 GPIO 实际驱动的段。

**实测结果与修正**：

| 段 | 原理图标注 | 实际硬件 | 修正 |
|----|-----------|---------|------|
| A | PB9 | PB9 | 不变 |
| B | PB12 | PC13 | 修正 |
| C | PB13 | PB13 | 不变 |
| D | PB14 | PB14 | 不变 |
| E | PB15 | PB15 | 不变 |
| F | PC13 | PC14 | 修正 |
| G | PC14 | PB12 | 修正 |
| DP | PC15 | PC15 | 不变 |

修正后 LED10 显示"0"验证通过，随后 1~9 逐字显示验证通过。

### 2.4 TIM2 定时器配置

| 参数 | 值 | 说明 |
|------|----|------|
| 时钟源 | TIM2 = 72 MHz | APB1(36MHz) × 2 |
| PSC | 71 | 计数时钟 1 MHz |
| ARR | 99 | 100us 更新周期 |
| 中断 | TIM2_IRQn | NVIC 抢占优先级 0 |

### 2.5 计时逻辑实现

`c
void TIM2_IRQHandler(void)
{
    if (TIM_GetITStatus(TIM2, TIM_IT_Update) != RESET)
    {
        TIM_ClearITPendingBit(TIM2, TIM_IT_Update);
        tick_100us++;              /* 100us 计数 */
        if (tick_100us >= 10)      /* 1ms 到达 */
        {
            tick_100us = 0;
            total_ms++;             /* 毫秒累计 */
        }
    }
}
`

中断中不做任何 GPIO 操作，仅更新 volatile 计时变量，显示刷新在 main 主循环完成。

### 2.6 显示刷新

主循环使用 __disable_irq()/__enable_irq() 原子读取 	otal_ms（uint32_t），再计算四个位值：

`c
LED1_Display((disp_total / 1)   % 10);   /* 毫秒个位 */
LED8_Display((disp_total / 10)  % 10);   /* 十毫秒位 */
LED9_Display((disp_total / 100) % 10);   /* 百毫秒位 */
LED10_Display((disp_total / 1000) % 10); /* 秒个位 */
`

LED10 小数点（PC15）在 LED10_Display 中常亮。

## 三、代码结构

`
app/
├── main.c            # 主循环：原子读取 total_ms，刷新四位数码管
├── gpio.c            # GPIO 初始化 + SEG_CODE 表 + 4 个 Display 函数
├── gpio.h
├── timer.c           # TIM2 100us 中断配置（PSC=71, ARR=99）
├── timer.h
├── delay.c           # SysTick 毫秒延时（备用）
├── delay.h
module/
├── stm32f10x_it.c    # TIM2_IRQHandler + tick_100us / total_ms
├── stm32f10x_it.h
├── stm32f10x_conf.h
project/MDK(V5)/
└── Project.uvprojx   # Keil 工程（已包含全部源文件）
`

## 四、测试与验证

| 测试项 | 方法 | 结果 |
|--------|------|------|
| 系统时钟 | 编译确认 72MHz 生效 | 通过 |
| LED10 引脚映射 | 逐引脚扫描 + 数字显示 | 通过（修正后 "0"~"9" 正常） |
| TIM2 中断周期 | 代码计算验证 | 100us（10kHz） |
| 计时功能 | 秒位每秒变化、毫秒位每毫秒变化 | 通过 |
| 编译 | Keil MDK-ARM | 0 error, 0 warning |

## 五、遗留问题与建议

1. 当前秒表无启动/暂停/清零按键功能，后续可接入按键扩展。
2. 	otal_ms 为 uint32_t，约 49.7 天后溢出，如需长时间计时可扩展为 64 位。
3. 建议后续将 LED10 的实测引脚映射同步到 PCB 原理图，避免后续维护混淆。

## 六、结论

本项目已完成全部要求：TIM2 100us 时间基准、tick_100us → total_ms 计时链路、四位数码管「秒.毫秒」显示、LED10 小数点常亮。经实测验证，显示功能完整、计时稳定，可作为电子秒表的基础版本交付。
